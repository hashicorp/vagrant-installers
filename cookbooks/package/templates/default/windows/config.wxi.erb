<?xml version="1.0" encoding="utf-8"?>
<Include>
  <?define VersionNumber="<%= node[:vagrant][:version] %>" ?>
  <?define DisplayVersionNumber="<%= node[:vagrant][:version] %>" ?>

  <!--
    Upgrade code must be unique per version installer.
    This is used to determine uninstall/reinstall cases.
  -->
  <?define UpgradeCode="<%= node[:package][:win][:upgrade_code] %>" ?>
</Include>