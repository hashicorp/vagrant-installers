$zipPath = "<%= @source_file_path %>"
$destination = "<%= @file_cache_dir %>"

$shell = New-Object -ComObject "Shell.Application"
$zip = $shell.NameSpace($zipPath)
foreach($item in $zip.items()) {
    $shell.Namespace($destination).copyhere($item)
}
